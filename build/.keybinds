;;; Joe Jevnik
;;; 2014.3.13
;;; Keybinding layout for the robot.

;; Defining symbolic names to the axis and trigger.
(define trigger (button 1))
(define x-axis  (axis   0))
(define y-axis  (axis   1))
(define twist   (axis   2))
(define slider  (axis   3))
(define hat-x   (axis   4))
(define hat-y   (axis   5))

;; Assigning operations to their keys.
;; To create an axis out of two buttons, use the (axis-pair a b) command.
(claw-open    (button 2))
(claw-close   trigger)
(claw-x       hat-x)
(claw-y       hat-y)
(rotate-z     (axis-pair 5 6))
(rotate-y     twist)
(turn-y       x-axis)
(transpose-x  y-axis)
(transpose-y  (axis-pair 3 4))
(thrust-mod   slider)
(laser-on     (button 7))
(laser-off    (button 8))
(laser-toggle (button 11) (button 12))
